Ich musste im PAC4200 doch noch die Stromrichtung der Messwandler ändern, damit Einspeisung und Bezug stimmen. Das bedeutet allerdings, dass die erhobenen Daten bis 20:20Uhr in der Stromrichtung nicht korrekt sind. Also Datenbank auf Tech und auf Primary nochmal zurücksetzen oder abhängige Werte invertieren. 

Wir werden die Zähler ab jetzt auch mit den Primär-SM-Zählern vergleichen. Die Abweichungen können uns zeigen, wie gut das SM misst. Im PAC werden ja true RMS bis H63 genau gemessen. iMS und SM schaffen das sicher nicht. Aufgabe der NQ-Datenbank ist also, die vorhandenen Zähler sinnvoll ins Monitoring zu spiegeln. Z.B. in den Ansichten Monat/Jahr/Gesamt zeigen die Tooltipps Bezug und Einspeisung - hier kann in Klammern der durch das PAC gemessene, für das Intervall gültige Wert stehen. Voraussetzung ist die Aggregation und Speicherung der Zählerstände in den exakten Intervallen. Also von 00:00 bis 00:00 Uhr die Tageswerte in Ansicht Monat, von 1.1., 00:00 Uhr bis 1.2. 00:00 Uhr die Monatswerte in Ansicht Jahr und von 1.1.27, 00:00 Uhr bis 1.1.2028 die Jahreswerte in der Ansicht Gesamt. 

Die schöne Live-Ansicht des PAC-Klons darf nicht direkt im PAC lesen. Vielleicht auf die RAM-DB als Datenquelle umstellen? Sind die Lesezugriffe auf die Datenbank schädlich für das kontinuierliche Beschreiben in Höchstgeschwindigkeit? Dann sollten wir einen kleinen Puffer im RAM schaffen, der die Daten für 1-2 Sekunden nochmal vorhält (dupliziert), nur um eine indirekte Echtzeitanzeige im PAC-Klon zu ermöglichen? Die Lesezugriffe sollten dann ausschließlich über diesen Puffer laufen? Sinnhaftigkeit prüfen und/oder andere Methoden recherchieren/erwägen. Oder ist es egal, ob noch 1-5 clients gleichzeitig direkt vom PAC lesen? Nach meiner Meinung ist aber eine alleinige Zugriffs-struktur "Pi4-Tech liest PAC" einfach und sicher. Alle Instanzen, die Daten vom PAC benötigen, können die von Tech bekommen! Ein 1-2s Polling des PAC-Klons auf den Puffer wäre völlig ausreichend, im Notfall 3 Sekunden.

Ich hoffe, es ist inzwischen fast/medium/slow implementiert, dass die im 200ms-Intervall erfassbaren Werte auch "fast" erfasst werden. Die Harmonischen UND die Netzfrequenz im 1s-Intervall als "medium" erfasst werden und alle Zählerdaten werden "slow" erfasst. Wir sollten "medium" zusätzlich die Grenzwert-Speicher abfragen, um daraus bei Bedarf einen direkten sofort-Alarm-Pfad per Mail zu initialisieren. Diese Speicher müssen aber konfiguriert werden. Dazu könntest du aus der DokumentationsPDF die nötigen Register ermitteln und einmalig fest belegen, so dass die im übernächsten Abschnitt (Qualität) beschriebenen Normen bei 90% Annäherung implementiert werden.

Dann ist die (4-stündliche (6 x täglich)) Aggregation zu prüfen, oder zu überarbeiten, so dass die fixpunktgesteuerten Zählereinträge dauerhaft gespeichert werden. Intervalle wie oben beschrieben Tag/Monat/Jahr. Alle Energiezähler speichern. Wirkleistung in zwei Richtungen (Bezug/Einspeisung), ebenso die Blindleistung. Die Scheinleistung ist nur einmal allgemein verfügbar - warum eigentlich? Das macht dann also 5x3 Spalten mit Werten +3 für den jeweiligen Zeit-Stempel. Aus dieser Quelle stammen dann die Spiegelungen in die Tooltipps im Monitoring. 

Sämtliche 200ms-Werte werden alle 4h gefiltert und aggregiert an die NQ-DB übertragen. Bitte an dieser Stelle sorgfältig überlegen, wie stark Daten reduziert werden und was wir wollen! Ziel ist es, aus den Messdaten die Qualität des Netzes abzuleiten. Was heißt das? Normgrenzwerte abgleichen mit IST-Werten und Warnungspfad implementieren. Warnstufen bei 50%/70%/90% der Endwerte. Grenzwerte für Spannungen (360-440V/207-253V), Ströme (konfigurierbar aus pv-config - maximaler Strom momentan 35A jede Ader), Frequenz (1%, kurz 47-52Hz), Spannungs-THD (8%). Die Darstellung im Chart sollte initial den Tag im 5min-Raster zeigen (analog zu der Echtzeit-Darstellung der SM-Werte). Die Auflösung auf 200ms für 300s (Chartbreite) ist ausschließlich Event-Schnipseln vorbehalten, die auf ihren Timestamp in der Tag-Darstellung verlinkt werden müssen! Da die Schnipsel kleiner sind, liegen die halt innerhalb der Trendlinie der aggregierten Daten direkt auf ihrem wirklichen Zeitraum innerhalb der 300s. Überschreitet ein Event einen 5min-Abschnitt, sollte einfach mit Navi-Pfeil rechts das angrenzende Intervall mit eingeblendetem Schnipsel angezeigt werden. Ich denke, wir vermeiden viel Verwirrung, wenn wir NICHT in den Tag hinein dynamische zoomen und panen. Feste Darstellung wäre mir erstmal lieber. Wir sind dann sowieso auf deine genialen Marker angewiesen, die uns die Extremwerte zeigen. Das Herauszoomen und dann panen sollte aber fließend möglich sein. Nur dass die kleinste Einheit quasi der Tag ist. Mit der Ausnahme, wie gesagt, dass (ausschließlich) markierte Events mit 200ms-Auflösung in einem oder bei längeren Events mehreren zusammenhängenden festen 5min-Charts angezeigt werden. Sicherheitsnetz ist hier die Prüfung auf sinngemäße Wiederholungen des Events, welche geeignet beschrieben werden, nicht aber als Schnipsel gespeichert werden. Außerdem sollte die maximale Schnipseldauer auf 60? oder besser 300s generell beschränkt werden. Die Erkennung ähnlicher Ereignisse betrifft die beteiligten Größen mit einem 30%igen Amplituden-Bereich und einem 10%igen Zeitbereich. Events sollten normal geloggt werden (max 10000 Zeilen), ähnliche Events werden nicht in Schnipseln gespeichert, wenn nicht mindestens 24 Stunden dazwischen liegen. Alle konfigurierbaren Parameter konfigurierbar machen ;)

Bevor weitere Analysen sinnvoll sind, müssen wir interne und externe Werte trennen. Das bedeutet, dass die Spannungsfälle auf geschaltete Ströme auf allen Leitern im Haushalt entsprechend neutralisiert werden, wenn es um die Erfassung externer Spannungsereignisse geht. Wenn dann externe Spannungsereignisse erfasst werden, können die wiederum benutzt werden, um sie für die internen Werte zu neutralisieren, damit das Bild von internen Vorgängen nicht durch externe Vorgänge beeinflusst ist.

FILTERN geht aber mit den nun sauber getrennten Werten noch viel weiter! Bei der Übernahme der Daten alle 4h werden natürlich die im 5min-Intervall aufgetretenen min/avg/max-Werte gespeichert. Zusätzlich sollten wir Transienten innerhalb der Intervalle messen und speichern (Es könnte ja sein, dass die Spannung in 5min gleichmäßig von 400 auf 420V steigt, kann aber auch sein, dass die Spannung innerhalb des Intervalls in 0,4s von 400 auf 420V springt ...). Dafür wäre wahrscheinlich eine Spalte für die Anzahl der Transienten pos, eine für negativ, eine für die durchschnittliche Anstiegsgeschwindigkeit und eine für die maximale Anstiegsgeschwindigkeit. Die Aggregation beschränken wir im kleinen Intervall auf nur ein 5min-Intervall/90 Tage, danach wie beim pv-system-collector!

Tja, Filtern bedeutet auch, zu entscheiden, welche Werte als Event beurteilt werden und dann als Schnipsel gespeichert werden müssen. Vielleicht für den Anfang alle Sprünge innerhalb von 1s auf 70% eines Grenzwertes (Qualität), aber auch sehr steile Transienten sind als Event zu bewerten. Der entsprechende Schnipsel - eben maximal 300s/oder 60 - muss dann in der Datenbank abgelegt werden und so sichtbar sein, dass im Chart ein entsprechender Marker erzeugt wird, der auf den Schnipsel zeigt ...

Und dann geht erst die Post ab: Mustererkennung auf das 4h-Intervall muss sofort laufen, bevor die Daten verworfen werden. Hier solltest du modular-offen coden, denn wir wissen noch nicht so genau, wonach und in welchen Werten wir suchen müssen. Entsprechende Marker für vermeintliche Muster (Erzeuger/Verbraucher-Schaltungen, Handels-Events...) sind für die Visualisierung in der Datenbank zu speichern. Die werden im Chart angezeigt.

Weitere Mustererkennungen müssen mit jedem 4h-Block über den dann verlängerten, gesamten Bereich ablaufen, um auch die VLF-Analyse zu starten. Je länger die Intervalle, um so größer die Lasten auf CPU und NVME. Da muss dann mit geringerer Priorität kontinuierlich gerechnet werden. Ergebnisse tauchen auch im Chart auf, ja. Aber im Wesentlichen werden wir auf Berichte angewiesen sein, die von der Mustererkennung erzeugt werden. Ziel A) Aufschwingen im europäischen Netz erkennen, Reflexionen an Netzgrenzen erkennen. Also müssen LF-Schwingungspakete erkannt und wiedererkannt werden, die durchs Netz wandern. Letztendlich sind jahreszeitliche, 
