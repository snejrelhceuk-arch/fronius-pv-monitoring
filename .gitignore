# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
.venv/
venv/
ENV/

# Datenbank
*.db
*.db-shm
*.db-wal
data_backup_*.db
data.db.*
*.bak_*

# Logs
*.log
/tmp/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Runtime-Dateien (werden von laufenden Services generiert)
collector.pid
wattpilot_collector.pid
automation_daemon.pid
config/battery_scheduler_state.json
config/battery_bms_checkpoints.json

# Lokale Kontroll-Datei (nicht im Repo)
ToExamine.md

# Host-Rolle (pro Pi individuell, siehe doc/DUAL_HOST_ARCHITECTURE.md)
.role
.state/

# Archiv & Backup (nur lokal, nicht im Repo)
archive/
backup/
2026-01-01-Archiv/
2025-12-30-Archiv/

# Zugangsdaten / Secrets
.secrets
*.secrets

# Wattpilot Dumps (enthalten WiFi-Passwörter, RFID-Daten etc.)
tools/wattpilot_dump_*.json
*.bak_*
backup/db/
*.empty_after_crash
*.bak

# Host-spezifische Laufzeitdaten (nicht versioniert)
*.db.before_restore_*

# Private Daten / Solarweb-Arbeitsverzeichnisse
E/
imports/

# Ollama Sync-State (wird lokal generiert)
ollama/.sync_state.json
