# Fronius PV-Monitoring - Python-Abhängigkeiten
# Installation: .venv/bin/pip install -r requirements.txt
#
# VERSIONS-PINNING
# =====================================
# App-Deps sind für venv-Reproduzierbarkeit gepinnt (== weiter unten).
# Update-Politik: doc/system/SYSTEM_ARCHITECTURE.md Abschnitt "Update-Strategie".
#
# Hinweis: Der frühere Sonderpfad (selbst kompiliertes SQLite, "nicht via apt
# upgraden", Pi4/armv7l/Python 3.9) ist mit dem Umstieg auf Pi5/64-bit hinfällig.
# SQLite + Python stammen jetzt aus Standard-Systempaketen und werden regulär
# mitaktualisiert. pip-Pins gezielt (nicht blind) heben, Kompatibilität nach
# einem Versions-Sprung kurz prüfen.
#
flask==1.1.2
gunicorn==23.0.0
requests==2.25.1
paho-mqtt==1.6.1
numpy==1.19.5
websockets==15.0.1
# pvlib und pandas: derzeit NICHT installiert (nur für solar_forecast.py geplant)
# pvlib>=0.10.4
# pandas>=2.0.0
